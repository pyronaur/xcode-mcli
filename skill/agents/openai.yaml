interface:
  display_name: "Xcode CLI"
  short_description: "Task needs the open Xcode window, issues, build logs, previews, snippets, or selective test runs"
  default_prompt: "Read when to interact with Xcode.app directly and task needs Xcode windows, issues, build logs, documentation search, previews, snippets, test discovery or selective test runs."
